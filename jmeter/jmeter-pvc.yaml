apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jmeter-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  volumeName: jmeter-pv
  resources:
    requests:
      storage: 1Gi